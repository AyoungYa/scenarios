
def basket_weight(basktets):
    """
    Give a map with values are list, sum each value and sorted, then return the key and sum value
    Args:
        basktets{map): A map of baksets, e.g. {"apple": [1, 2.3, 4, 5], "orange": [3], "banana": [4, 5, 6]}

    Returns:
        result(list): A list of basket and it's weight,
        e.g. [{"name": "banana", "weight": 15}, {"name": "apple", "weight": 12.3}, {"name": "orange", "weight": 3}]

    """
    # TODO: implement this function
    # Note: Do not change the existing code
    result = None
    return result


